# This repository only needs workflows in the root .github/workflows folder.
# The active workflow is located at ../../.github/workflows/build-noname-mobile.yml.
# This placeholder file is kept so local tooling that expects a workflow in this
# directory continues to work, but GitHub Actions will ignore it.
